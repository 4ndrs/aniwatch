query Anime($id: Int!) {
  Page {
    media(type: ANIME,id: $id) {
      id
      season
      seasonYear
      description
      bannerImage
      title { romaji }
      coverImage { extraLarge }
    }
  }
}
